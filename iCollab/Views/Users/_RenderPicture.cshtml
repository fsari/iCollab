@model Model.Attachment
           
@if (Model != null)
{
    <img src="@Model.Path" class="user-image img-responsive img-thumbnail" alt="User Image" width="75" />
}
else
{
    <img src="/Assets/dist/img/sb.png" class="user-image" alt="User Image" />
}
 
